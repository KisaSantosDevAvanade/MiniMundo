@model MiniMundo.Models.Credencial

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Credencial</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CredencialID" />
            <div class="form-group">
                <label asp-for="NomeAdm" class="control-label"></label>
                <input asp-for="NomeAdm" class="form-control" />
                <span asp-validation-for="NomeAdm" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NivelAcesso" class="control-label"></label>
                <input asp-for="NivelAcesso" class="form-control" />
                <span asp-validation-for="NivelAcesso" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
